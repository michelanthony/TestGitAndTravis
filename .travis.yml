#language: python
language: cpp

language: cpp
sudo: required
dist: trusty
compiler:
- gcc
os:
- linux
  
script: 
  - echo $PATH
  - echo $HOME
  - gcc -o run.exe hello.cpp 
  
after_success:
  - ./run.exe > logfile.log
  - echo logfile.log
